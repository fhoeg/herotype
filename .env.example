# Vercel AI Gateway key — powers POST /api/style (Claude Haiku 4.5).
# Set in the Vercel dashboard / `vercel env add AI_GATEWAY_API_KEY`, and here
# (as .env.local) for local `vercel dev`. Without it the app falls back to the
# local keyword parser, so the UI still works.
AI_GATEWAY_API_KEY=
